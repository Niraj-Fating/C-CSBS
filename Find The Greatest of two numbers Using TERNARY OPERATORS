#include <stdio.h>

void main() 
{
    int a,b;
    printf("\n Enter any two numbers:");
    scanf("%d %d",&a,&b);


      (a > b) ? printf("%d is greater",a): ( (a<b) ? printf("%d is greater",b): printf("Both are Equal")
    );
}
