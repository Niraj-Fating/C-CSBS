#include <stdio.h>

int main()
{
   int num,i;
   
   printf("Enter any Number:\n");
   scanf("%d",&num);
   
   i = 1;
   
   do
   {
       printf("\n%d x %d = %d",num,i,num*i);
       i++;
   }while(i<=10);
}
